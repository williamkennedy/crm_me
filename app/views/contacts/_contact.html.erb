<li id="<%= dom_id contact %>" class='px-6 py-4 flex flex-row justify-between'>
  <span class='font-bold'>
    <%= contact.name %>
  </span>
  <%= link_to contact, class: "btn btn-primary #{'hidden' if params[:action] == 'show'}" do %>
    <%= heroicon "eye", options: { class: 'h-5 w-5'} %>
  <% end %>
</li>
